query GetCats {
  cats {
    id
    lat
    lng
    creator {
      id
      name
    }
  }
}

query GetCat($catId: String!) {
  cat(id: $catId) {
    id
    lat
    lng
    images {
      id
      path
    }
    creator {
      id
      name
    }
  }
}
