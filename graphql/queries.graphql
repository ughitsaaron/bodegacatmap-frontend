query GetCats {
  cats {
    id
    ...LatLng
  }
}

query GetCat($catId: String!) {
  cat(id: $catId) {
    id
    lat
    lng
    insertedAt
    images {
      id
      path
    }
    creator {
      id
      name
    }
  }
}
